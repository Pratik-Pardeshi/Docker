# Use the official Nginx image as the base image
FROM nginx:latest

# Copy your web application code into the Nginx server's default public HTML directory
COPY ./my-web-app /usr/share/nginx/html

# Expose port 80 to allow outside access to your web server
EXPOSE 80

